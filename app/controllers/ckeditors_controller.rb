class CkeditorsController < ApplicationController
  def upload
    #ckeditor = Ckeditor.create(:picture=>params[:upload])
    #on_upload(ckeditor.picture.url)
  end
  
  def browser
    #@images = Ckeditor.all
    #render :layout=>"upload"
  end
end
